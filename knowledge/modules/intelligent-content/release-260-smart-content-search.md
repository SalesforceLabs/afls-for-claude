# Admin - Smart Content Search (Data Cloud Configuration)

Welcome everyone to this series of enablement recordings. My name is Eva Miguel, and in this video, I'll guide you through how to configure a smart content search using Data Cloud. So before setting up a smart content search, make sure that you have Data Cloud, and it is fully set up for your Salesforce org.

The first step is to install the Life Sciences Data Kit. To do that, we go to the setup. You search for "data kit kits". We search for "Life Sciences Data Kit". So this kit sets up the required presentation data streams, data model objects, and data lake objects that support a smart content search. So it is already installed on my org. You just need to install it and deploy it. After the installation, you need to go to Data Cloud in the App Launcher.

And verify that the following presentation data streams were created. So you need to verify that "Presentation Home," "Presentation Page Home," and "Presentation Link Page Home" are here and have been automatically created. If you don't see these data streams, you'll need to create them manually. So we have these three: "Presentation Home" and "Presentation Page Home". These three are automatically created.

Now the next step is to set up the file attachment ingestion so that Data Cloud can ingest files attached to Presentation Page records and include their content in the search index. So, here in the Data Stream, we click on "New". We select "Salesforce CRM". We click "Next". We are going to ingest three objects that are Content Document, Content Version, and Content Document Link. So you click on "View Object", and we are going to select Content. These three: Content Document, Content Link, and Content Version. Click "Next".

Here we are going to review the fields for the object that are included in our data stream. We can deselect the fields that aren't required, but here, so we can keep all of them. We are just going to deselect this setting option. So this one for Content Document and Content Version, the setting option. Here, this one, and then click "Next". We keep everything by default, and then just click on "Deploy". I'm not going to do it because I have already done it. To review that this has been successful, we go to the—let me just close it—to the Data Lake Object. And we need to see those "Content Document Link Home," "Content Version Home," and "Content Document Home".

This configuration creates data streams and maps DLOs and DMOs to the following Salesforce objects: Content Document, Content Document Link, and Content Version Home. Once file ingestion is configured, the next step is to create the vector search index using the advanced setup option.

So here, we search for the search index, that is already—it is here—Search Index. We click on "New", "Advanced Setup," "Next". So on the source DMO page, we choose "Vector Search". Then we select the appropriate data space that it is "Default". And then we just check our object that we need, that it is "Presentation Page," and we click "Next". At this stage, so we are going to add some fields here—at least one. But there is no real impact on selecting what fields we are going to select here, but we just need to select at least one. Then we can have "Include File Attachments". So we add it—all the file extensions—here. And then we click "Next".

So at this stage, we will select the model used for vectorization. The model you choose directly impacts how content is understood and matched, and therefore determines the quality and the relevance of the search results. We have multiples one here. So you choose the right model that will ensure that the smart content search returns results based on semantic meaning, not just keyword matching. I have just selected this one, and then I click "Next".

This one we can skip. And then once everything is configured, I just click "Save". I will not do it because I have already done it. And then you will see your index that it will be here. What is important is this API name because we need to copy it and to paste in the Presentation Setting. So, once I have already configured my Data Cloud part, I go back to my Life Science Commercial App in the Admin Console. Select Intelligent Content, Presentation Settings.

Under search settings, we need to enable the "Use Data Cloud to search presentations content" checkbox. So then we have to enter here the Search Index API Name. So this is the name of the search index that we have just created in Data Cloud. The next step is to select the file types you want to include in your search index for presentation content. So we recommend indexing at least HTML files. PDF files are indexed automatically. Finally, you save your changes.

Next, we need to assign the permission set to allow users to search within presentations content in the Content Library or in the presentation player. We need to assign them the Data Cloud User permission set. So we are going to the Setup. We look for our user, and we make sure that we have the Data Cloud User permission set.

Another thing that we need to make sure is configured is we go to the Permission Set. We select "Data Cloud Salesforce Connector". App Permissions. Here we make sure "Does query non-vector files" is checked. Then we go back to the permission set again, to the list. This time we look for Data Cloud Architect. Data Cloud Architect. Here we do Data Cloud Data Space Management. Edit. And if this is not checked, you need to check it to enable it, and then save.

And this is all for the configuration part. So when this setup is complete, the smart content search will be enabled, allowing users to search across presentation content using Data Cloud-powered capabilities on both iPad and web. Thank you.
